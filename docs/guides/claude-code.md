# Claude Code Guide

Claude Code can be used as a runner when a lane prompt is written for that tool.

Example:

```bash
scripts/wave-run-detached docs-lane "claude -p \"Update public docs from templates/wave-plan.md\""
```

The same lane, state pack, forbidden-path, receipt, and milestone rules apply.

